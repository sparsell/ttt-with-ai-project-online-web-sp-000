module Players
  class Computer < Player

    def move(board)#returns a valid position for the computer to move => need #computer_move method
      # input = number.random(from 0 to 8) + 1
      #check number is not already taken (array number or space number? - where does the conversion happen?)
      # input = gets.to_i

    end

  end

end
